# Contributing

This repository currently contains a product and architecture specification. Keep changes scoped to the documented milestone, preserve the privacy and portability constraints, and do not add implementation before it is approved.

Every milestone is gated on its required GitHub Actions workflow being green on `main`. Use `corismix` in public attribution. Do not commit credentials, local paths, personal identity, or generated provider payloads.
